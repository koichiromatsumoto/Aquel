<% if model.errors.any? %>
    <% model.errors.full_messages.each do |message| %>
        <div id="alert" class="alert-dismissible alert alert-danger col-md-offset-3 col-md-6" >
			<a class="close" data-dismiss="alert" aria-label="Close">×</a>
			<%= message %>
		</div>
    <% end %>
<% end %>